---
project:
  title: My iRODS project
  description: Title of my irods project
  version: "1.0"
  rapydo: 0.7.3

variables:

  env:

    FRONTEND_FRAMEWORK: angular
    BACKEND_BUILD_MODE: backendirods
    AUTH_SERVICE: sqlalchemy

    ACTIVATE_ALCHEMY: 1
    ACTIVATE_POSTGRES: 1

    ALCHEMY_DBS: 'ICAT SQL_API'

  backend:
    credentials:
      roles:
        user: normal_user
        internal: staff_user
        admin: admin_root
        default: normal_user