---
project:
  title: My MongoDB project
  description: Title of my mongodb project
  version: "1.0"
  rapydo: 0.7.1

variables:

  env:
    ACTIVATE_MONGODB: 1
    AUTH_SERVICE: mongo

  backend:
    credentials:
      username: "user@nomail.org"
      password: "test"
      roles:
        user: normal_user
        internal: staff_user
        admin: admin_root
        default: normal_user
  frontend:
    framework: angular
