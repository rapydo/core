---
project:
  title: My MongoDB project
  description: Title of my mongodb project
  version: "1.0"

variables:
  env:
    GRAPHDB_PASSWORD: chooseapassword
    IRODS_CHUNKSIZE: 1048576
  backend:
    credentials:
      username: "user@nomail.org"
      password: "test"
      roles:
        user: normal_user
        internal: staff_user
        admin: admin_root
        default: normal_user
  frontend:
    enable: true

releases:
  '1.0':
    type: beta
    rapydo: 0.6.0
    status: developing
