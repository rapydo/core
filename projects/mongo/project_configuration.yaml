---
project:
  title: My MongoDB project
  description: Title of my mongodb project
  version: "1.0"
  rapydo: 0.7.3

variables:

  env:
    FRONTEND_FRAMEWORK: angular
    ACTIVATE_RABBIT: 1
    ACTIVATE_MONGODB: 1
    AUTH_SERVICE: mongo

  backend:
    credentials:
      roles:
        user: normal_user
        internal: staff_user
        admin: admin_root
        default: normal_user
