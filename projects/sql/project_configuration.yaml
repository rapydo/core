---
project:
  title: My SQL project
  description: Title of my sql project
  version: "RAPyDo framework v0.5.2-beta"

variables:
  repos:
    rapydo:
      branch: 0.6.0
    utils:
      branch: 0.6.0
    backend:
      branch: 0.6.0
    build-templates:
      branch: 0.6.0
  env:
    GRAPHDB_PASSWORD: chooseapassword
    IRODS_CHUNKSIZE: 1048576
  backend:
    credentials:
      username: "user@nomail.org"
      password: "test"
      roles:
        user: normal_user
        internal: staff_user
        admin: admin_root
        default: normal_user
  frontend:
    enable: true

