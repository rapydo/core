version: '3'
#######################
# Link services inside API

services:
  backend:
    # command: sleep infinity
    environment:
      APP_MODE: debug
      FLASK_DEBUG: 1
      DEBUG_ENDPOINTS: 1
      DEBUG_LEVEL: VERBOSE
      ACTIVATE: 1
    ports:
      - 8080:5000

  frontend:
    ports:
      - 80:80
    environment:
      APP_MODE: debug
      BACKEND_PORT: 8080
      ACTIVATE: 1

  neo4j:
    ports:
      - 9090:7474
      - 7687:7687

  swaggerui:
    ports:
      - 7777:80
