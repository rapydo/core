---
project:
  title: My project
  description: Title of my project

variables:
  env:
    GRAPHDB_PASSWORD: chooseapassword
  backend:
    credentials:
      username: "user@nomail.org"
      password: "test"
      roles:
        user: normal_user
        internal: staff_user
        admin: admin_root
        default: normal_user
  frontend:
    enable: true

