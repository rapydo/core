---
project:
  title: My project
  description: Title of my project

controller:
  commands:
    initialization:
      description: Try to init your project. Fails if already done once?
      command: initialize
      service: backend
      user: root

variables:
  env:
    COMPOSE_PROJECT_NAME: rapydodemo
    PROJECT_DOMAIN: localhost
    GRAPHDB_PASSWORD: chooseapassword
  backend:
    credentials:
      username: "user@nomail.org"
      password: "test"
      roles:
        user: normal_user
        internal: staff_user
        admin: admin_root
        default: normal_user
  frontend:
    enable: true
