development: True
# project: your_project
mode: debug
# mode: production
hostname: localhost
log-level: DEBUG

project_configuration:
  variables:
    env:
      SMTP_ADMIN: your@email.org
      # other variables

